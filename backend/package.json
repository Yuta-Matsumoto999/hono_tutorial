{
  "name": "app",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "generate": "bun drizzle-kit generate --config ./src/db/drizzle/local/drizzle.config.ts",
    "migrate": "bun drizzle-kit migrate --config ./src/db/drizzle/local/drizzle.config.ts"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.2",
    "drizzle-orm": "^0.33.0",
    "hono": "^4.6.3",
    "postgres": "^3.4.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.24.2"
  }
}